{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "IBCDenomInfo",
  "description": "This is the data stored against the registered IBC denom - just used for indexing for now it can be expanded in future",
  "type": "object",
  "required": [
    "base_denom"
  ],
  "properties": {
    "base_denom": {
      "description": "base denom identifier e.g. juno. Probably it will be the Bech32 HRP",
      "type": "string"
    }
  }
}
